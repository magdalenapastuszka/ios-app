import SwiftUI

extension Color {
    static let backgroundColor = Color(uiColor: .backgroundColor)
    static let primaryColor = Color(uiColor: .primaryColor)
    static let textColor = Color(uiColor: .textColor)
    static let buttonTitleColor = Color(uiColor: .buttonTitleColor)
    static let secondaryTextColor = Color(uiColor: .secondaryTextColor)
}
